{% extends 'base.html.twig' %}

{% block title %}{{ 'connexion_page'|trans }}{% endblock %}

{% block body %}
    <div class="circle circle-1"></div>
    <div class="circle circle-2"></div>
    <div class="circle circle-3"></div>
    <div class="circle circle-4"></div>
    <div class="circle circle-5"></div>
    <h1 class="h3 mb-3 font-weight-normal">{{ 'connexion_page'|trans }}</h1>
    <div class='form_container'>
    <form method="post">
        {% if error %}
            <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
        {% endif %}

        {% if app.user %}
            <div class="mb-3">
                {{ 'logged_as'|trans }} {{ app.user.userIdentifier }}, <a href="{{ path('app_logout') }}">{{ 'logged_out'|trans }}</a>
            </div>
        {% endif %}
        <div>
        <label for="username">{{ 'label.email'|trans }}</label>
        <input type="email" value="{{ last_username }}" name="_username" id="username" class="form-control" autocomplete="email" required autofocus>
        </div>
        <div>
        <label for="password">{{ 'label.password'|trans }}</label>
        <input type="password" name="_password" id="password" class="form-control" autocomplete="current-password" required>
        </div>
        <input type="hidden" name="_csrf_token"
               value="{{ csrf_token('authenticate') }}"
        >

        {#
            Uncomment this section and add a remember_me option below your firewall to activate remember me functionality.
            See https://symfony.com/doc/current/security/remember_me.html

            <div class="checkbox mb-3">
                <input type="checkbox" name="_remember_me" id="_remember_me">
                <label for="_remember_me">Remember me</label>
            </div>
        #}

        <button class="btn btn-lg btn-primary" type="submit">
            {{ 'connexion_page'|trans }}
        </button>
    </form>
    </div>
    <div class="inscription-container">
    <hr>
    <h2>{{ 'other.creation'|trans }}</h2>
    <a href="{{ path('app_register') }}">{{ 'inscription_page'|trans }}</a>
    </div>
{% endblock %}
